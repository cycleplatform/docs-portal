Choose **Stateful** or **Stateless** for your container.

`Stateful` containers use a mounted volume to persist data between restarts.
`Stateless` containers do not persist any applications generated state between restarts.


